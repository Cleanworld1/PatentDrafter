/** 이미지 AI·도면 작성 프롬프트 공통 — 크기·밀도·블록 규칙 */
export const DRAWING_LAYOUT_COMPACT_RULES = `[도면 크기·밀도 — 필수]
- 한 장에 구성 블록은 **8개 이하**를 권장한다. 과도한 세분·중첩·작은 글씨 나열은 금지.
- 직사각형(네모) 블록: 구성요소 **이름**만 짧게(2~6단어). 블록 안 긴 문장·단락·수치 목록 금지.
- 마름모 블록: **조건·분기**만 한 줄(예: "임계값 초과?").
- 화살표·연결선: 동작·데이터명을 짧게(동사 1~3어). 장문 라벨 금지.
- A4 한 장(또는 정사각형) 비율, **충분한 여백**을 두고 배치하라고 명시.
- 10단계 이상 흐름은 **5~7개** 상위 단계로 묶고 하위는 생략.
- 흑백 특허 선도, 글자·도형 깨짐·미완성·플레이스홀더(TBD 등) 금지.`;

export function getDrawingLayoutRulesBlock(): string {
  return DRAWING_LAYOUT_COMPACT_RULES;
}
